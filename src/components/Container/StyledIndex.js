import styled from "styled-components";

const StyledIndex = styled.div`
    .content {
        min-width: 462px;
        max-width: 1200px;
        width: 100%;
        margin: 0 auto;
        padding: 1rem;
    }
`;

export default StyledIndex;