import styled from "styled-components";

const StyledIndex = styled.div`
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 1rem;
    }
`;

export default StyledIndex;